.PHONY: install dev build preview

# First-time setup.
install:
	npm install

# Dev server with hot reload (prints a localhost URL).
dev:
	npm run dev

# Production bundle -> dist/
build:
	npm run build

# Serve the built dist/
preview:
	npm run preview
