[tool.poetry]
name = "combatden-app-deploy"
version = "0.1.0"
description = "Boto3 deploy scripts for the CombatDen CRM web app (S3 + CloudFront)"
authors = ["Jesse <jessemusa2@gmail.com>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
boto3 = "^1.35"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
