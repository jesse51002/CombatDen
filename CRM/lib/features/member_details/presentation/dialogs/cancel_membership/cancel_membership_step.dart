/// The two steps of the Cancel Membership wizard.
///
/// [participant] is shown only when the member has linked
/// accounts; otherwise the flow opens on [memberships].
enum CancelMembershipStep { participant, memberships }
